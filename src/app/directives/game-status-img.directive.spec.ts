import { GameStatusImgDirective } from './game-status-img.directive';

describe('GameStatusImgDirective', () => {
  it('should create an instance', () => {
    const directive = new GameStatusImgDirective();
    expect(directive).toBeTruthy();
  });
});
